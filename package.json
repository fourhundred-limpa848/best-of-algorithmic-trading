{
  "name": "best-of-algorithmic-trading",
  "private": true,
  "type": "module",
  "description": "Curated best-of list; TypeScript tooling validates projects.yaml",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "validate": "tsx src/validate-projects.ts",
    "check": "npm run build && npm run validate"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "ioredis-om": "^5.10.2", 
    "typescript": "^5.7.2",
    "yaml": "^2.6.1",
    "zod": "^3.24.1"
  }
}
